# Import model
import numpy as np
import torch
import torch.nn as nn
import sys


class Sequence(nn.Module):
    def __init__()：
        


def import_knn_models(PATH:str,num_):

    return model.eval(),labels_map
